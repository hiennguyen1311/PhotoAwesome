import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  iconActive: {
    borderRadius: 50,
    height: 44,
    width: 44,
  },
  iconStyle: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  imageStyle: {
    width: 20,
    height: 20,
  },
  svgContainer: {
    alignItems: 'center',
    justifyContent: 'center',
  },
});
